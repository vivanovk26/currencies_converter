<manifest
    package="com.vivanov.currenciesconverter"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name="com.vivanov.currenciesconverter.config.app.CurrenciesConverterApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CurrenciesConverterTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name="com.vivanov.currenciesconverter.presentation.main.CurrencyRatesActivity"
            android:launchMode="singleTop"
            android:theme="@style/CurrenciesConverterTheme.Launcher"
            android:windowSoftInputMode="adjustPan">

            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
